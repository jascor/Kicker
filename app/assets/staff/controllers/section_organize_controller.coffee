Kicker.SectionOrganizeController = Ember.Controller.extend(
  actions:
    ###
      @todo How can we make this callback available to all components without passing it multiple times?
    ###
    didDropItem: (data) ->

)