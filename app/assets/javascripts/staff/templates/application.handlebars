{{#if isAuthenticated}}
    {{ render 'staff/partials/header' }}

    {{ render 'staff/partials/sidebar' }}
{{/if}}

{{ outlet }}

{{ popup-notifications-container notifications=unreadNotifications }}